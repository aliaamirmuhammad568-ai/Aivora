# Copy this file to .env
# Points the app at the deployed backend by default. For local testing
# against `npm run server` on your machine, set this to your computer's
# LAN IP (not localhost — your phone can't reach your computer's localhost),
# e.g. http://192.168.1.23:3001
EXPO_PUBLIC_API_URL=https://aivora-1qva.onrender.com
